{
  "name": "danmull-in",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/hash-agent-skill.mjs && node scripts/check-web-bot-auth-directory.mjs && vite build && node scripts/inline-css.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
